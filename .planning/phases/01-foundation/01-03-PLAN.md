---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified: []
autonomous: false
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - PIPE-05

must_haves:
  truths:
    - "docker compose up starts both services with no errors"
    - "GET http://localhost:8000/health returns {status: ok}"
    - "Frontend is reachable at http://localhost:5173"
    - "POST /campaigns creates a campaign record"
    - "GET /campaigns returns the created campaign"
    - "All automated tests pass"
  artifacts:
    - path: "backend/app/models.py"
      provides: "Campaign, Contact, PipelineLog tables"
    - path: "backend/app/pipeline/__init__.py"
      provides: "Provider factory functions"
  key_links:
    - from: "docker-compose.yml"
      to: "backend and frontend services"
      via: "docker compose up --build"
      pattern: "compose up"
---

<objective>
Verify the complete Phase 1 foundation: both Docker services start, the Campaign API works end-to-end, and the provider registry resolves correctly.

Purpose: Gate before Phase 2 begins. Phase 2 pipeline work depends on the data layer and provider registry being confirmed working together.
Output: Human-confirmed working stack.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all automated tests</name>
  <files></files>
  <action>
    Run the full backend test suite. All tests from Plan 01 and Plan 02 must pass:
    - tests/test_models.py (Campaign, Contact, PipelineLog model tests)
    - tests/test_campaigns_api.py (GET/POST /campaigns, GET /campaigns/{id})
    - tests/test_provider_registry.py (provider factory functions)

    If any test fails, diagnose and fix the root cause before proceeding.
  </action>
  <verify>
    <automated>cd /Users/vinethsiriwardana/swiftdeeds-outreach-pipeline/backend && python -m pytest tests/ -v 2>&1</automated>
  </verify>
  <done>All tests in tests/ directory pass with no failures or errors.</done>
</task>

<task type="auto">
  <name>Task 2: Docker Compose smoke test</name>
  <files></files>
  <action>
    1. Start the stack in detached mode: `docker compose up --build -d`
    2. Wait for backend health: poll `curl -s http://localhost:8000/health` until it returns {"status":"ok"} or timeout after 30s
    3. Test the Campaign API end-to-end:
       - POST http://localhost:8000/campaigns with body {"name": "Test Campaign", "target_criteria": {"industry": "SaaS", "job_title": "CTO", "location": "US"}} — expect 201
       - GET http://localhost:8000/campaigns — expect array containing the created campaign
       - GET http://localhost:8000/campaigns/{id} from step above — expect campaign + empty contacts + empty logs
    4. Verify frontend is reachable: `curl -s -o /dev/null -w "%{http_code}" http://localhost:5173` — expect 200
    5. Stop the stack: `docker compose down`

    If any step fails, inspect container logs (`docker compose logs backend` or `docker compose logs frontend`) and fix before reporting.
  </action>
  <verify>
    <automated>cd /Users/vinethsiriwardana/swiftdeeds-outreach-pipeline && docker compose up --build -d && sleep 5 && curl -s http://localhost:8000/health && docker compose down 2>&1</automated>
  </verify>
  <done>docker compose up starts cleanly, /health returns ok, campaign POST and GET work, frontend serves on 5173, docker compose down cleans up.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of Phase 1 foundation</name>
  <files></files>
  <action>
    Present the following verification steps to the user and wait for confirmation:

    1. Run: `cd /Users/vinethsiriwardana/swiftdeeds-outreach-pipeline && docker compose up --build`
    2. Visit http://localhost:5173 — should show "Outreach Pipeline" heading
    3. In a separate terminal: `curl -s http://localhost:8000/health` — expect {"status":"ok"}
    4. `curl -s -X POST http://localhost:8000/campaigns -H "Content-Type: application/json" -d '{"name":"Test","target_criteria":{"industry":"SaaS"}}' | python3 -m json.tool`
       — expect 201 with id, name="Test", status="pending"
    5. `curl -s http://localhost:8000/campaigns | python3 -m json.tool` — expect array with one campaign
    6. When satisfied, run `docker compose down`
  </action>
  <what-built>
    Phase 1 Foundation — complete:
    - SQLModel data layer: Campaign, Contact, PipelineLog tables
    - Campaign CRUD API: POST/GET /campaigns, GET /campaigns/{id}
    - Provider registry: get_sourcing_provider/enrichment/campaign driven by env vars
    - Docker Compose: both services build and start
    - Frontend scaffold: React/Vite app reachable at localhost:5173
  </what-built>
  <how-to-verify>
    1. Run: `cd /Users/vinethsiriwardana/swiftdeeds-outreach-pipeline && docker compose up --build`
    2. Visit http://localhost:5173 — should show "Outreach Pipeline" heading
    3. In a separate terminal: `curl -s http://localhost:8000/health` — expect {"status":"ok"}
    4. `curl -s -X POST http://localhost:8000/campaigns -H "Content-Type: application/json" -d '{"name":"Test","target_criteria":{"industry":"SaaS"}}' | python3 -m json.tool`
       — expect 201 with id, name="Test", status="pending"
    5. `curl -s http://localhost:8000/campaigns | python3 -m json.tool` — expect array with one campaign
    6. When satisfied, run `docker compose down`
  </how-to-verify>
  <verify>
    <automated>MISSING — this is a human verification checkpoint</automated>
  </verify>
  <done>User has confirmed the stack starts and the Campaign API works end-to-end.</done>
  <resume-signal>Type "approved" to advance to Phase 2, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Full test suite passes. Docker Compose starts both services. Campaign API accepts POST and returns data via GET. Frontend serves at port 5173.
</verification>

<success_criteria>
- `python -m pytest tests/ -v` — all tests pass (0 failures)
- `docker compose up --build -d` — both backend and frontend start without errors
- `curl http://localhost:8000/health` — returns {"status":"ok"}
- `POST /campaigns` — returns 201 with campaign id and status="pending"
- `GET /campaigns` — returns array
- `curl http://localhost:5173` — returns HTTP 200
- Human confirms everything looks correct
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
